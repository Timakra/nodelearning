<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>{{ title }}</title>
    </head>
   
    <body>
        {{> header }}
        <h1>{{ headerText }}</h1>
        <p>{{ text }}</p>

        {{> footer}}
    </body>
</html>
